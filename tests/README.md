# Root Test Assets

Most tests in this repository live next to their modules:

- `apps/CodexChatApp/Tests/CodexChatAppTests`
- `packages/*/Tests`

The root `tests/` folder currently stores shared fixtures and integration assets used by deterministic smoke/protocol tests.
