# Desktop Cleanup Mod

First-party mod that triggers the native `desktop.cleanup` action.

## Behavior

- Shows a one-click `Preview Cleanup` action in Mods bar.
- Native action generates a preview first.
- Execution requires explicit confirmation and writes an undo manifest.
