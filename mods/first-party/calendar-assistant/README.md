# Calendar Assistant Mod

First-party mod that triggers the native `calendar.today` action.

## Behavior

- Read-only calendar lookup (today/next range).
- Uses native macOS Calendar permission flow.
- No write operations in v1.
